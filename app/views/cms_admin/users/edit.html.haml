= link_to 'Add New User', new_cms_admin_user_path, :class => 'big_button'

%h2 Editing User

- form_for :user, :url => {:action => :update}, :html => {:method => :put, :multipart => true}  do |form|
  = render :partial => form
  .submit
    = form.submit 'Update User'
    or 
    = link_to 'Return to List', cms_admin_users_path