%h1 Dashboard for #{current_user.name}

%table.user_profile
  %tr
    %td.avatar= image_tag current_user.avatar.url
    %td
      .name= current_user.name
      - unless current_user.relationship.blank?
        .relationship (#{current_user.relationship})
      .profile= current_user.profile